# v1.6.1 Contract Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Align release verification with the current four published ZIP roles and ensure whole-chapter rewrites reuse the `prose-writing` Skill contract.

**Architecture:** Keep the current verifier module and workflow split. Replace historical release-role assumptions with explicit current archive roles, and narrow the workflow integration rule so only the whole-chapter rewrite branch in workflow 04 may invoke the prose Skill.

**Tech Stack:** Python 3.11, `zipfile`, `hashlib`, `pathlib`, pytest/unittest-style repository tests, Markdown workflow contracts.

## Global Constraints

- Do not change prose style guidance, planning heuristics, light-check thresholds, or the one-repair limit.
- Preserve strict path-safety checks for ZIP members and manifest entries.
- The checksum file must cover exactly the four current ZIP assets.
- Whole-chapter rewrite passes only the prose-minimal packet and scene execution card to `mode=prose-writing`.

---

### Task 1: Release verifier regression coverage

**Files:**
- Create: `tests/test_release_integrity_contract.py`
- Modify: `tools/release_integrity.py`

**Interfaces:**
- Consumes: `verify_release_directory(release_dir)`
- Produces: the same `list[str]` error contract, with current release archive roles.

- [ ] **Step 1: Write failing tests**

Create synthetic ZIPs and SHA256SUMS covering `codex-novel-workflow-portable-v1.6.1.zip`, `serial-fiction-workflow-v1.6.1-official.zip`, `skill.zip`, and `writing-serial-fiction-v1.6.1.zip`. Assert the current verifier rejects the valid fixture before implementation.

- [ ] **Step 2: Run the focused test and verify RED**

Run: `python -m pytest -q tests/test_release_integrity_contract.py`
Expected: failure because the current verifier still requires overlay/profile ZIPs.

- [ ] **Step 3: Implement current release roles**

Keep portable inner-manifest verification. Add role-specific shape validation for source and Skill ZIPs, retain duplicate/path-safety/CRC checks, and require the outer checksum file to list all four ZIPs exactly once.

- [ ] **Step 4: Run the focused test and verify GREEN**

Run: `python -m pytest -q tests/test_release_integrity_contract.py`
Expected: PASS.

### Task 2: Whole-chapter rewrite contract regression coverage

**Files:**
- Modify: `tests/skill-evals/test_workflow_skill_integration.py`
- Modify: `工作流/04_修正.md`

**Interfaces:**
- Consumes: `$writing-serial-fiction mode=prose-writing`
- Produces: a rewritten chapter from exactly `工作区/第N章_正文最小包.md` and `工作区/第N章_场景执行卡.md`, followed by one light-check.

- [ ] **Step 1: Write failing integration assertions**

Replace the blanket workflow-04 Skill prohibition with assertions that workflow 03 stays Skill-free, while the `整章重写` section of workflow 04 invokes `mode=prose-writing`, names only the two approved inputs, and contains failure/output-shape gates.

- [ ] **Step 2: Run the focused test and verify RED**

Run: `python -m pytest -q tests/skill-evals/test_workflow_skill_integration.py`
Expected: failure because the current whole-chapter rewrite branch does not invoke the Skill.

- [ ] **Step 3: Update the workflow contract**

Edit only the `整章重写` branch to invoke the prose Skill and mirror first-pass failure handling without allowing a second repair loop.

- [ ] **Step 4: Run the focused test and verify GREEN**

Run: `python -m pytest -q tests/skill-evals/test_workflow_skill_integration.py`
Expected: PASS.

### Task 3: Full verification and review

**Files:**
- Verify all changed files only; no new behavior.

- [ ] **Step 1: Run the full test suite**

Run: `python -m pytest -q`
Expected: all tests pass with no generated cache artifacts retained in the clean test tree.

- [ ] **Step 2: Review branch diff**

Confirm changes are limited to release verification, whole-chapter rewrite contract, regression tests, and these design/plan docs.

- [ ] **Step 3: Open a Draft PR**

Use a concise title and body documenting the two fixed contract gaps and verification results.