# 小说章节生成控制器

本项目用于按固定流程生成长篇小说章节。执行任务时，先读取本文件，再按需读取 `工作流/` 中对应文件。不要一次读取全部工作流。

## 一、长期资料

- `01_剧情主线.md`：全书方向、核心冲突、关键转折和最终结果。
- `02_连续状态表.md`：写作开始前已经成立的客观事实。
- `03_大纲.md`：当前卷或阶段的目标与节奏。
- `04_章节细纲.md`：本章必须发生的事件、结果和结尾落点。
- `05_写作规则卡.md`：长期稳定的文风和表现规则。

资料优先级：

1. 已确认的连续状态和客观事实；
2. 章节细纲规定的事件与结果；
3. 场景执行卡规定的展开方式；
4. 写作风格偏好。

场景执行卡只能细化“怎样展开”，不得改变“发生什么”和“结果是什么”。

## 二、用户命令

### `准备第N章`

执行前必须满足以下条件之一：

- 当前阶段为 `空闲`；
- 上一章的当前阶段为 `本章完成`、`状态表已更新` 为 `是`，并且 N 是 `04_章节细纲.md` 中存在的合法下一章。

其他阶段拒绝执行，不得覆盖或跳过当前活动章节。

读取并执行 `工作流/01_准备资料.md`。

该工作流生成资料包后，必须显式调用
`$writing-serial-fiction mode=chapter-planning` 填写执行卡中的本章任务和场景部分。
控制器只负责路由、校验、组装和保存，不在本文件复制 Skill 的写作方法。

完成后必须：

- 创建 `工作区/第N章_资料包.md`；
- 创建 `工作区/第N章_正文最小包.md`；
- 创建 `工作区/第N章_场景执行卡.md`；
- 更新 `工作区/当前任务.md`；
- 将阶段设置为 `等待确认执行卡`；
- 停止，不生成正文。

### `确认执行卡，生成正文` 或 `生成正文`

只有 `工作区/当前任务.md` 的阶段为 `等待确认执行卡` 时才能执行。

当前任务必须已经登记并实际存在本章资料包、正文最小包和场景执行卡。控制器只校验资料包存在，不把资料包内容交给正文生成任务。

迁移项目若已处于 `等待确认执行卡` 但缺少正文最小包，拒绝生成正文，将当前阶段退回 `空闲`，要求重新执行 `准备第N章` 并重新确认新执行卡；不得临时从旧资料包拼装正文输入。

依次完成：

1. 将当前阶段设置为 `正在生成正文`，然后读取并执行 `工作流/02_生成正文.md`；该工作流必须显式调用 `$writing-serial-fiction mode=prose-writing`；
2. 正文初稿保存成功后，读取并执行 `工作流/03_轻检.md`；轻检除事实硬错外，只在有跨段落证据时识别成片机械化表达；
3. 根据轻检结论进入相应分支。

处理轻检结论前必须先读取 `修正次数`。修正次数已经达到 1 时，重新轻检只有 `通过` 可以生成正式正文并更新状态表；任何未通过结论一律进入 `等待作者决定`。

结论为 `通过`：

- 确认状态补丁中的每项都有正文依据；
- 将初稿转为 `正文/第N章.md`；
- 将状态补丁合并进 `02_连续状态表.md`；
- 将当前阶段设置为 `本章完成`；
- 将 `状态表已更新` 设置为 `是`。

首次轻检且修正次数为 `0`，结论为 `局部修补`：

- 不生成正式正文；
- 不修改连续状态表；
- 将当前阶段设置为 `等待局部修正`；
- 停止。

首次轻检且修正次数为 `0`，结论为 `重写场景` 或 `整章重写`：

- 不生成正式正文；
- 不修改连续状态表；
- 将当前阶段设置为 `等待作者决定`；
- 停止，不自动重写。

### `开始修正`

只有当前阶段为 `等待局部修正` 且 `修正次数` 为 `0` 时才能执行。读取并执行 `工作流/04_修正.md`。

修正次数已经达到 1 时，拒绝再次执行任何自动修正。

一次命令最多进行一轮局部修补和一次重新轻检。重新轻检仍未通过时必须停止，不得循环修改。

### `确认重写场景`

只有当前阶段为 `等待作者决定`、轻检结论为 `重写场景` 且 `修正次数` 为 `0` 时才能执行。读取 `工作流/04_修正.md`，只重写报告指定的场景，然后重新轻检一次。

### `确认整章重写`

只有当前阶段为 `等待作者决定`、轻检结论为 `整章重写` 且 `修正次数` 为 `0` 时才能执行。先保留现有初稿，再由 `工作流/04_修正.md` 的整章重写分支使用正文最小包和执行卡显式调用 `$writing-serial-fiction mode=prose-writing` 生成一次新正文，随后重新轻检。不得连续自动重写。

### `准备下一章`

只有当前阶段为 `本章完成` 且 `状态表已更新` 为 `是` 时才能执行。

将当前章节加一，然后执行对应章节的准备流程。如果章节细纲中不存在下一章，停止并说明原因。

### `查看当前任务`

任何阶段都可以执行。只读取并汇报 `工作区/当前任务.md`，不得修改任何文件。

只汇报：当前章节、当前阶段、已有文件、阻塞原因和当前可用命令。

## 三、任务状态

允许的阶段只有：

- `空闲`
- `等待确认执行卡`
- `正在生成正文`
- `等待轻检`
- `等待局部修正`
- `等待作者决定`
- `本章完成`
- `阻塞冲突`

每次阶段变化都必须更新 `工作区/当前任务.md`。命令与阶段不匹配时，拒绝执行并说明当前阶段和可用命令。

## 四、上下文隔离

- 资料准备阶段只读取本章相关内容，生成规划／审计资料包、正文最小包和场景执行卡，不完整复述长期资料。
- 正文阶段只读取本章正文最小包和场景执行卡，禁止读取规划／审计资料包。
- 轻检阶段只读取本章资料包、场景执行卡、正文初稿和状态表中的相关记录。
- 局部修补和场景重写只读取轻检报告、需修改的片段及其前后衔接；整章重写只读取正文最小包和场景执行卡。
- 工作流 01 调用 `chapter-planning`，工作流 02 调用 `prose-writing`。
- 工作流 03 不调用 `$writing-serial-fiction`；工作流 04 只有整章重写分支调用 `prose-writing`，局部修补和场景重写不调用 Skill；`readonly-diagnosis` 只用于独立只读诊断，不进入自动章节流程。
- 如果当前环境支持独立子任务，让正文生成与轻检由不同的独立任务完成。
- 如果不支持独立子任务，仍必须严格分阶段执行，轻检不得引用正文生成时的创作解释。

## 五、文件保护

- `01_剧情主线.md`、`03_大纲.md`、`04_章节细纲.md`、`05_写作规则卡.md`默认只读。
- 轻检通过前不得修改 `02_连续状态表.md`。
- 未通过轻检的正文只能保存在 `工作区/`，不得写入 `正文/`。
- 已存在的正式正文不得覆盖；发现同名文件时停止并报告。
- 状态表更新只修改补丁涉及的字段，并标注来源章节。
- 不把人物计划、推测或尚未发生的事件写入状态表。

## 六、复杂化限制

- 一个阶段只完成一种任务。
- 不重新策划全书，不输出备选方案。
- 不擅自增加重要人物、支线、核心设定或长期伏笔。
- 不为追求“深刻”增加象征系统、哲学讨论或宏大主题。
- 小型信息空缺采用改动最少的自然处理。
- 不进行无条件全文润色；只有轻检给出跨段落证据的机械化问题，才允许一次最小删除、合并或缩短。
- 不进行无限自检或修订循环。
- Skill 返回 `INPUT_ERROR:` 时不得把错误文本保存为执行卡或初稿；按对应工作流恢复到安全阶段。
- Skill 返回单行 `BLOCKED:` 时不得继续后续阶段，当前阶段设置为 `阻塞冲突`。

## 七、最终汇报

每次命令结束只汇报：

- 当前章节；
- 完成了哪些文件；
- 当前阶段；
- 是否存在阻塞；
- 用户下一条可用命令。
