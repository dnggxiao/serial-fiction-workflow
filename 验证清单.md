# 工作流验证清单

在放入真实小说内容后，让 Codex 按顺序验证。验证期间不要生成正式正文。

## 一、结构检查

- [ ] `项目清单.md` 中所有必需文件存在。
- [ ] `AGENTS.md` 位于项目根目录。
- [ ] 四个工作流文件没有合并成一个长文件。
- [ ] `工作区/当前任务.md` 的初始阶段为 `空闲`。
- [ ] `正文/` 中不存在准备测试章节的同名文件。
- [ ] 场景执行卡规则始终为 500～800 字，使用篇幅权重与视角偏压，不含场景精确字数或“必须呈现”。
- [ ] `模板/正文最小包模板.md` 存在，正文工作流禁止读取规划／审计资料包。
- [ ] Skill 中存在 `references/natural-prose-patterns.md`，正文与诊断模式都会加载它。

## 二、资料检查

- [ ] 剧情主线包含全书方向和最终结果。
- [ ] 连续状态表只记录已发生事实。
- [ ] 大纲可以定位当前章节所在阶段。
- [ ] 章节细纲有明确章号、必须事件、结果和结尾落点。
- [ ] 写作规则卡不超过 8 条主要规则。
- [ ] 写作规则卡包含 2～4 个作者认可的短文风锚点，并注明来源章与只学习的特征。
- [ ] 候选稿、历史废稿和未通过初稿没有被用作正向文风锚点。
- [ ] 常驻人物声纹来自既有正式正文或作者明确要求，不是临时编造的口癖。

## 三、命令路由测试

### 测试A：准备阶段

输入：`准备第001章`

预期：

- 只生成规划／审计资料包、正文最小包和执行卡；
- 正文最小包包含去重后的决策状态、静默禁区、视角偏压与人物声纹；
- 执行卡把方法翻译为具体动作，不使用“读者承诺、状态变化、目标可行性”等课程语言；
- 不生成初稿或正式正文；
- 当前阶段变为 `等待确认执行卡`。

### 测试B：阶段保护

在初始阶段直接输入：`生成正文`

预期：

- 拒绝执行；
- 说明当前阶段不允许；
- 不创建任何正文文件。

### 测试C：通过分支

使用一份没有明确问题的测试初稿执行轻检。

预期：

- 轻检结论为 `通过`；
- 初稿进入正式正文目录；
- 状态补丁合并进状态表；
- 当前阶段变为 `本章完成`。

### 测试D：事实失败分支

在测试初稿中故意制造一个与状态表冲突的事实。

预期：

- 轻检结论不是 `通过`；
- 不创建正式正文；
- 不修改状态表；
- 当前阶段进入修正或等待决定。

### 测试E：机械化问题分支

准备一份事件正确但存在以下成片模式的测试稿：同一规则在三个分离位置被动作、对白和旁白重复解释；三名重要人物都使用可互换的完整分析台词。

预期：

- 轻检必须列出至少三处分离证据，不能只写“像AI”；
- 结论为 `局部修补` 或 `重写场景`，不得因为孤立句子判整章失败；
- 修补优先删除、合并和缩短，不新增景物、心理或解释补字数；
- 修补后只重新轻检一次。

### 测试F：叙事自证与状态渗漏误报保护

准备一份只有一处概括句、一个谨慎技术限定或一名说话较理性的角色的测试稿。

预期：

- 必要技术链、一次必要限定、真实状态变化和固定结尾中的否定动作不得被误判；
- 不得仅凭某个词、句长或否定句数量判机械化问题；
- 没有其他明确问题时结论为 `通过`。

### 测试G：循环限制

让局部修正后的稿件仍保留一个明确问题。

预期：

- 重新轻检后停止；
- 不自动进入第二轮修正；
- 当前阶段变为 `等待作者决定`；
- 正式正文仍为 `未生成`；
- 状态表已更新仍为 `否`；
- 不再开放 `开始修正`。

### 测试H：场景重写后的循环限制

让场景重写后的稿件仍保留一个明确问题。

预期：

- 重新轻检后停止；
- 无论新结论是哪一种未通过结论，当前阶段都变为 `等待作者决定`；
- 不生成正式正文，不更新状态表，不再开放任何自动修正。

### 测试I：任务路径回填

预期：

- 轻检报告保存后，当前任务记录轻检报告实际路径、轻检结论和对应阶段；
- 轻检通过后，当前任务记录正式正文实际路径、状态表已更新=`是`、当前阶段=`本章完成`。

## 四、正式使用前结论

只有 A～I 全部符合预期，才开始使用真实章节。

## 开源发布边界

- [ ] 根目录和独立 Skill 均包含可识别的 `LICENSE`、`NOTICE.md` 与 `THIRD_PARTY_NOTICES.md`。
- [ ] 公开仓库不包含真实小说正文、私人设定、未授权文风样本或不应公开的工作区文件。
- [ ] 第三方资料只保留必要摘要和来源链接，不复制课程原文、付费内容或受限制材料。
- [ ] README 明确区分 MIT 授权的模板材料与不自动开源的用户内容。
