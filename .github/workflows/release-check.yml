name: Release repair staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - name: Await repair payload
        run: echo "Repair payload is being staged."
